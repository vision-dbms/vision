struct program_description_t {
    char const *auditFile, *auditEntry, *compilationLogin, *compilationDate, *platform, *release, *source;
} ProgramDescription = {
  AuditFile_, AuditEntry_, CompilationLogin_, CompilationDate_, Platform_, Release_, Source_
};

