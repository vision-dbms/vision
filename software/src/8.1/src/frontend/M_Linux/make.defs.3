# This file loads in the makefile defaults from make.gcc but overrides the makemake variables. This is altogether too hackish but hopefully someday we can get rid of makemake entirely.
include ../M_Linux/make.gcc

# Override for C projects.
#--objectrule--#        gcc $(CFLAGS) $(CDEFS) $(CINCS) -o $@ -c
#--objectsuffix--#	$B
#--targetsuffix--#	$B
#--tarulesuffix--#	$B
#--buildtargets--#	targets

# Override CBASE
override CBASE = -fexceptions -Wno-trigraphs

LIBS += -lncurses -lcrypt
